// Runtime-bound factory split out of Raven Gear runtime.
// Behavior is intentionally preserved while calculator/planning code moves under its feature folder.

export function createPlanningMathModule(runtimeContext){
  const source = "// ===== src/math-plans.js =====\n// Calculator plan math: target items, remaining requirements, and side totals.\n\n    function targetItemsFromRandomPlan(plan,p){\n      return DomainPlanning.targetItemsFromRandomPlanDomain(plan,p,chestLevels);\n    }\n\n    function targetItemsFromPlanAndChoice(plan,guaranteed,p){\n      return DomainPlanning.targetItemsFromPlanAndChoiceDomain(plan,guaranteed,p,chestLevels);\n    }\n\n    function calculateAdditionalOwnedFromPlan(plan, p){\n      return DomainPlanning.calculateAdditionalOwnedFromPlanDomain(plan,p);\n    }\n\n    function buildLeftoverHelperRows(plan, samePerItemProb, oppositePerItemProb){\n      const sameRows=[];\n      const oppositeRows=[];\n      const sameFractionTotals={};\n      const oppositeFractionTotals={};\n\n      for(let i=0; i<2; i++){\n        const raw={};\n        chestLevels.forEach(level=>{\n          const qty=safeNum(plan[level]);\n          if(qty>0) raw[level]=qty*samePerItemProb;\n        });\n        const simplified=simplifyBucketDecimal(raw);\n        const display=displayRowFromSimplified(simplified);\n        const fractions=fractionalRemainderFromSimplified(simplified);\n        sameRows.push(display);\n        addObjects(sameFractionTotals,fractions);\n      }\n\n      const sameRemainderDisplay=displayRowFromSimplified(simplifyBucketDecimal(sameFractionTotals));\n      sameRows.push(sameRemainderDisplay);\n\n      for(let i=0; i<3; i++){\n        const raw={};\n        chestLevels.forEach(level=>{\n          const qty=safeNum(plan[level]);\n          if(qty>0) raw[level]=qty*oppositePerItemProb;\n        });\n        const simplified=simplifyBucketDecimal(raw);\n        const display=displayRowFromSimplified(simplified);\n        const fractions=fractionalRemainderFromSimplified(simplified);\n        oppositeRows.push(display);\n        addObjects(oppositeFractionTotals,fractions);\n      }\n\n      const oppositeRemainderDisplay=displayRowFromSimplified(simplifyBucketDecimal(oppositeFractionTotals));\n      oppositeRows.push(oppositeRemainderDisplay);\n\n      return {\n        sameRows,\n        oppositeRows,\n        sameTotal:addDisplayRows(sameRows),\n        oppositeTotal:addDisplayRows(oppositeRows),\n        allTotal:addDisplayRows([...sameRows,...oppositeRows])\n      };\n    }\n\n    function rawRandomSideTotals(plan, selectedSideValue){\n      const left={};\n      const right={};\n      chestLevels.forEach(level=>{\n        const qty=safeNum(plan[level]);\n        if(qty<=0) return;\n\n        if(selectedSideValue===\"left\"){\n          // Target item is left. Left leftovers are the other 2 left items (2/9 each);\n          // right leftovers are the 3 right items (1/9 each).\n          left[level]=(left[level]||0)+excelRoundWhole(qty*(4/9));\n          right[level]=(right[level]||0)+excelRoundWhole(qty*(3/9));\n        }else{\n          // Target item is right. Right leftovers are the other 2 right items (1/9 each);\n          // left leftovers are the 3 left items (2/9 each).\n          right[level]=(right[level]||0)+excelRoundWhole(qty*(2/9));\n          left[level]=(left[level]||0)+excelRoundWhole(qty*(6/9));\n        }\n      });\n      return {left,right};\n    }\n\n    function calculateAdditionalOwnedFromPlanExcludingLevel(plan, p, excludedLevel){\n      return DomainPlanning.calculateAdditionalOwnedFromPlanExcludingLevelDomain(plan,p,excludedLevel);\n    }\n\n    function deterministicSideTotalsFromRandomPlan(plan){\n      return DomainPlanning.deterministicSideTotalsFromRandomPlanDomain(plan,chestLevels);\n    }\n\n    function targetItemsRawFromRandomPlan(plan,p){\n      return DomainPlanning.targetItemsRawFromRandomPlanDomain(plan,p,chestLevels);\n    }\n\n    function nontargetSideTotalsFromRandomPlan(plan,side,p){\n      return DomainPlanning.nontargetSideTotalsFromRandomPlanDomain(plan,side,p,chestLevels);\n    }\n\n    function calculateAdditionalOwnedFromPlanAndChoice(plan, guaranteed, p){\n      return DomainPlanning.calculateAdditionalOwnedFromPlanAndChoiceDomain(plan,guaranteed,p);\n    }\n\n    function rawTargetAdditionsFromPlanAndChoice(plan, guaranteed, p){\n      return DomainPlanning.rawTargetAdditionsFromPlanAndChoiceDomain(plan,guaranteed,p);\n    }\n\n    function calculateEffectiveOwnedForRemainingItems(owned, plan, guaranteed, p){\n      return DomainPlanning.calculateEffectiveOwnedForRemainingItemsDomain(owned,plan,guaranteed,p);\n    }\n\n    function rawTargetAdditionsFromPlanExcludingLevel(plan, p, excludedLevel){\n      return DomainPlanning.rawTargetAdditionsFromPlanExcludingLevelDomain(plan,p,excludedLevel);\n    }\n\n    function calculateEffectiveOwnedForRemainingChestColumn(owned, plan, guaranteed, p, excludedLevel){\n      return DomainPlanning.calculateEffectiveOwnedForRemainingChestColumnDomain(owned,plan,guaranteed,p,excludedLevel);\n    }\n\n    function rawTargetFromRandomPlan(plan,p){\n      return DomainPlanning.rawTargetFromRandomPlanDomain(plan,p,chestLevels);\n    }\n\n    function rawChoiceObject(choiceObj){\n      return DomainPlanning.rawChoiceObjectDomain(choiceObj);\n    }\n\n    function finalBreakdownContribution(outputs){\n      return DomainPlanning.finalBreakdownContributionDomain(outputs);\n    }\n\n\n\n\n";
  const build = new Function("ctx", `
    with (ctx) {
      ${source}
      return {
        targetItemsFromRandomPlan: typeof targetItemsFromRandomPlan!=="undefined" ? targetItemsFromRandomPlan : undefined,
        targetItemsFromPlanAndChoice: typeof targetItemsFromPlanAndChoice!=="undefined" ? targetItemsFromPlanAndChoice : undefined,
        calculateAdditionalOwnedFromPlan: typeof calculateAdditionalOwnedFromPlan!=="undefined" ? calculateAdditionalOwnedFromPlan : undefined,
        buildLeftoverHelperRows: typeof buildLeftoverHelperRows!=="undefined" ? buildLeftoverHelperRows : undefined,
        rawRandomSideTotals: typeof rawRandomSideTotals!=="undefined" ? rawRandomSideTotals : undefined,
        calculateAdditionalOwnedFromPlanExcludingLevel: typeof calculateAdditionalOwnedFromPlanExcludingLevel!=="undefined" ? calculateAdditionalOwnedFromPlanExcludingLevel : undefined,
        deterministicSideTotalsFromRandomPlan: typeof deterministicSideTotalsFromRandomPlan!=="undefined" ? deterministicSideTotalsFromRandomPlan : undefined,
        targetItemsRawFromRandomPlan: typeof targetItemsRawFromRandomPlan!=="undefined" ? targetItemsRawFromRandomPlan : undefined,
        nontargetSideTotalsFromRandomPlan: typeof nontargetSideTotalsFromRandomPlan!=="undefined" ? nontargetSideTotalsFromRandomPlan : undefined,
        calculateAdditionalOwnedFromPlanAndChoice: typeof calculateAdditionalOwnedFromPlanAndChoice!=="undefined" ? calculateAdditionalOwnedFromPlanAndChoice : undefined,
        rawTargetAdditionsFromPlanAndChoice: typeof rawTargetAdditionsFromPlanAndChoice!=="undefined" ? rawTargetAdditionsFromPlanAndChoice : undefined,
        calculateEffectiveOwnedForRemainingItems: typeof calculateEffectiveOwnedForRemainingItems!=="undefined" ? calculateEffectiveOwnedForRemainingItems : undefined,
        rawTargetAdditionsFromPlanExcludingLevel: typeof rawTargetAdditionsFromPlanExcludingLevel!=="undefined" ? rawTargetAdditionsFromPlanExcludingLevel : undefined,
        calculateEffectiveOwnedForRemainingChestColumn: typeof calculateEffectiveOwnedForRemainingChestColumn!=="undefined" ? calculateEffectiveOwnedForRemainingChestColumn : undefined,
        rawTargetFromRandomPlan: typeof rawTargetFromRandomPlan!=="undefined" ? rawTargetFromRandomPlan : undefined,
        rawChoiceObject: typeof rawChoiceObject!=="undefined" ? rawChoiceObject : undefined,
        finalBreakdownContribution: typeof finalBreakdownContribution!=="undefined" ? finalBreakdownContribution : undefined
      };
    }
  `);
  return build(runtimeContext);
}
