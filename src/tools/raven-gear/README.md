# Raven Gear Tool

Raven Gear calculator, inventory, scanner, What If, bundles, and Raven Gear data backup code live here.

Import/export backup scope is Raven Gear data only, not the whole LAP Hub.

Runtime note: `raven-gear-runtime.js` owns the current Raven Gear runtime surface. `src/app/core/app-runtime.js` is only a small hub-level facade.
