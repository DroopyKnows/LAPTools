// Compatibility wrapper for the old root-level `domain-backup.js` module.
// Real implementation lives at `../tools/raven-gear/backups/raven-gear-backup.js`.

export * from "../tools/raven-gear/backups/raven-gear-backup.js";
