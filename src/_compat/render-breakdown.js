// Compatibility wrapper for the old root-level `render-breakdown.js` module.
// Real implementation lives at `../render/breakdown.js`.

export * from "../tools/raven-gear/shared/render/breakdown-render.js";
