// Compatibility wrapper for the old root-level `render-inventory.js` module.
// Real implementation lives at `../render/inventory.js`.

export * from "../tools/raven-gear/inventory/inventory-render.js";
