// Compatibility wrapper for the old root-level `app.js` entry fragment.
// Use `src/main.js` for app startup; boot helper lives in `src/core/app-boot.js`.

export { bootApp } from "../app/boot/app-boot.js";
